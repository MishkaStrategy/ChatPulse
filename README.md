<div align="center">
  <img src="chrome-extension/assets/logo.svg" width="112" alt="ChatPulse logo">
  <h1>ChatPulse</h1>
  <p><strong>Локальное расширение Google Chrome, которое безопасно продолжает выбранные чаты ChatGPT.</strong></p>

  ![Chrome](https://img.shields.io/badge/Google_Chrome-Manifest_V3-4285F4?logo=googlechrome&logoColor=white)
  ![Version](https://img.shields.io/badge/version-0.5.5_beta-7C5CFC)
  ![Tests](https://img.shields.io/badge/CI-5%2F5_passed-2EA44F)
  ![License](https://img.shields.io/badge/license-MIT-blue)
</div>

## Что делает ChatPulse

ChatPulse работает внутри уже авторизованного профиля Chrome и следит только за выбранными страницами `chatgpt.com`. Когда ответ ассистента завершён и остаётся неизменным полный интервал, расширение отправляет команду:

```text
продолжай и не останавливайся до технического лимита
```

Команду можно изменить в настройках.

## Возможности

- работа с обычным входом ChatGPT через Google в Chrome;
- несколько сохранённых чатов;
- интервалы от 30 секунд до 24 часов;
- последовательная проверка вкладок;
- безопасная первая проверка без отправки;
- настраиваемая стоп-фраза, автоматически отключающая только тот чат, где она появилась в завершённом ответе ассистента;
- опциональные Telegram-уведомления после автоматического продолжения;
- защита `at-most-once` от повторной отправки одному ответу;
- автоматическое восстановление выгруженных, frozen и неотвечающих вкладок;
- плановое обновление безопасных неактивных чатов каждые 5–15 минут;
- защита активной вкладки, текущей генерации и пользовательского черновика;
- восстановление фоновой генерации, зависшей более 20 минут;
- повторный freshness-preflight непосредственно перед отправкой;
- добавление последнего использованного чата из полного интерфейса;
- открытие уже существующей вкладки без создания дубля;
- локальный журнал событий с причиной восстановления;
- скины **macOS** и **ChatPulse Preview**;
- версия интерфейса автоматически берётся из Manifest.

## Стоп-фраза

В полном интерфейсе задайте поле **Стоп-фраза ответа**. Правило выключено, пока поле пустое. ChatPulse проверяет только последний завершённый ответ ассистента: Unicode нормализуется в NFKC, регистр не учитывается, переносы строк и повторяющиеся пробелы схлопываются. Если нормализованный ответ содержит нормализованную фразу, расширение отключает только этот чат и не отправляет ему команду продолжения.

Пользовательские сообщения и незавершённая генерация не учитываются. Значение стоп-фразы и текст ответа не записываются в журнал. Чтобы вернуть остановленный чат, нажмите **Включить** — ChatPulse создаст для него новый безопасный baseline.

## Telegram-уведомления

Telegram выключен по умолчанию. В полном интерфейсе укажите `Chat ID` и bot token от BotFather, включите переключатель **Уведомлять** и сохраните настройки. Chrome отдельно запросит доступ только к `https://api.telegram.org/*`. Для проверки есть кнопка **Отправить тест**.

После автоматического продолжения ChatPulse может отправить в Telegram:

- название отслеживаемого чата;
- результат отправки команды — подтверждённая отправка или факт клика с уже заблокированным повтором.

Текст ответа ChatGPT, URL разговора, стоп-фраза и сама команда в Telegram не отправляются. Bot token хранится только локально в отдельной конфигурации `chrome.storage.local`, не возвращается в публичное состояние интерфейса и после сохранения не показывается обратно.

Ошибка Telegram не влияет на основную автоматику: к моменту уведомления `at-most-once` уже зафиксирован, поэтому timeout/ошибка Bot API не могут повторно отправить команду ChatGPT. Обычные настройки сохраняются независимо от состояния Telegram permission.

## Установка

1. Скачайте репозиторий или ZIP из GitHub Actions.
2. Распакуйте архив и сохраните папку в постоянном месте.
3. Откройте в Chrome адрес `chrome://extensions`.
4. Включите **Режим разработчика**.
5. Нажмите **Загрузить распакованное расширение**.
6. Выберите папку `chrome-extension`, внутри которой находится `manifest.json`.
7. Закрепите ChatPulse через меню расширений Chrome.

Подпись Apple, Safari Extension и отдельное macOS-приложение не требуются.

## Первый запуск

1. Войдите в ChatGPT в том же профиле Chrome.
2. Откройте конкретный чат с адресом вида `https://chatgpt.com/c/...`.
3. Нажмите значок ChatPulse.
4. Выберите **Добавить текущий чат**.
5. Установите интервал и нажмите **Запустить**.

В полном интерфейсе кнопка добавления выбирает последний использованный конкретный чат ChatGPT, даже когда сама страница настроек является активной вкладкой.

Первая проверка только фиксирует текущий ответ. Если ответ не изменился, следующая проверка может отправить команду ровно один раз.

## Как работает восстановление вкладок

Для управляемых вкладок ChatPulse пытается отключить автоматическую выгрузку Chrome. Перед чтением DOM расширение проверяет состояния `discarded` и `frozen`, доступность content script и наличие ошибки страницы. При необходимости вкладка безопасно обновляется, после чего ChatPulse ждёт загрузку и гидратацию интерфейса ChatGPT.

Неактивные вкладки без черновика и без текущей генерации периодически обновляются. Активную вкладку и заполненное поле ввода ChatPulse автоматически не перезагружает.

## Приватность и разрешения

Все настройки и служебные отпечатки хранятся локально в `chrome.storage.local`. Расширение не содержит телеметрии, серверной части, рекламы, внешнего ИИ и платных API.

Постоянные разрешения:

- `alarms` — плановые проверки;
- `storage` — локальные настройки и журнал;
- `tabs` — поиск, открытие и восстановление сохранённых чатов;
- `scripting` — восстановление content script после обновления вкладки.

Постоянный доступ к страницам ограничен доменами `chatgpt.com` и `chat.openai.com`. `api.telegram.org` находится только в `optional_host_permissions` и запрашивается лишь при включении/тесте Telegram. Расширение не запрашивает `cookies`, `history`, `webRequest`, `debugger`, `nativeMessaging` или `<all_urls>`.

Подробнее: [PRIVACY.md](PRIVACY.md) и [SECURITY.md](SECURITY.md).

## Проверка разработки

Требуется Node.js 20 или новее:

```bash
npm run audit:extension
npm run package:beta
```

`audit:extension` проверяет синтаксис, stop-phrase/control-revision semantics, service worker, Telegram privacy/permission boundary, восстановление вкладок, выбор последнего чата, отсутствие дублей, `at-most-once`, Manifest V3 и минимальность постоянных разрешений. GitHub Actions повторяет полный аудит в пяти независимых циклах.

`package:beta` создаёт воспроизводимый `ChatPulse-Chrome-v0.5.5-beta.zip`, его SHA-256 и `ChatPulse-Chrome-v0.5.5-source-manifest.txt` с отдельным SHA-256. ZIP строится из отсортированного набора файлов расширения с фиксированными метаданными времени и прав.

## Статус

Текущая версия — **0.5.5 beta**. Она сохраняет все гарантии 0.5.4 и добавляет опциональные Telegram-уведомления с отдельным permission boundary.

Предыдущая нативная версия для macOS на WebKit удалена из основной ветки проекта. `main` содержит только Chrome-расширение.

## Лицензия

MIT. См. [LICENSE](LICENSE).

ChatPulse не является продуктом OpenAI и не аффилирован с OpenAI.
